package courseProject;

public class SavingsAccount extends Account {

    public SavingsAccount() {
        setServiceFee(0.25);
        setInterestRate(5);
    }

    @Override
    public void deposit(double amount) {
        setBalance(getBalanceValue() + amount - getServiceFee());
    }

    @Override
    public void withdrawal(double amount) {
        if (amount + getServiceFee() > getBalanceValue()) {
            System.out.println("Cannot withdraw more than balance for Savings Account.");
        } else {
            setBalance(getBalanceValue() - amount - getServiceFee());
        }
    }

    @Override
    public double balance() {
        return getBalanceValue();
    }

    public void addInterest() {
        setBalance(getBalanceValue() + getBalanceValue() * getInterestRate() / 100);
    }
}
